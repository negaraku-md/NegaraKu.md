---
import BaseLayout from '@/layouts/BaseLayout.astro';
import MapView from '@/components/MapView.astro';
import { t } from '@/lib/i18n';
const locale = 'ms';
---

<BaseLayout locale={locale} title={t('nav.map', locale)} path="/map">
  <MapView locale={locale} />
</BaseLayout>
