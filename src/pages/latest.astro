---
import BaseLayout from '@/layouts/BaseLayout.astro';
import LatestView from '@/components/LatestView.astro';
import { t } from '@/lib/i18n';
const locale = 'ms';
---

<BaseLayout locale={locale} title={t('nav.latest', locale)} path="/latest">
  <LatestView locale={locale} />
</BaseLayout>
